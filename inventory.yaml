all:
  children:
    manager:
      hosts:
       docker-manager
    worker:
      hosts:
        docker-worker1
    nginxloadbalancer:
      hosts:
        nginx
  vars:
    ansible_python_interpreter: "/usr/bin/python3"