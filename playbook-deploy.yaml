- hosts: manager
  become: true
  tasks:
  - name: Copy docker-compose
    copy:
      src: docker-compose.yaml
      dest: docker-compose.yaml

- hosts: nginx
  become: true
  roles:
  - loadbalancer